version: '3.9'
services:
  ssh.cyrhades.fr:
    build: ./.docker/ssh
  webssh.cyrhades.fr:
    build: ./.docker/webssh
    ports:
      - ${PORT}:8888
  hyper-packer.cyrhades.fr:
    image: anssi/fcsc2022-reverse-hyper-packer:latest
    ports:
      - ${PORT_SSH}:4000
